import 'package:flutter/material.dart';

class SafetyOnTheStreetsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Safety on the Streets'),
      ),
      body: Center(
        child: Text('Content for Safety on the Streets'),
      ),
    );
  }
}
