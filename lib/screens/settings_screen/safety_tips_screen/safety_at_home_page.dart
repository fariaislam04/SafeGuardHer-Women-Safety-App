import 'package:flutter/material.dart';

class SafetyAtHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Safety at Home'),
      ),
      body: Center(
        child: Text('Content for Safety at Home'),
      ),
    );
  }
}
